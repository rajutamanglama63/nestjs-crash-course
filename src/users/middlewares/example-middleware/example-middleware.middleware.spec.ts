import { ExampleMiddlewareMiddleware } from './example-middleware.middleware';

describe('ExampleMiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new ExampleMiddlewareMiddleware()).toBeDefined();
  });
});
